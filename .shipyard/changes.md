# Change requests

<!-- File one with `/shipyard change "…"` (or the dashboard). The PM triages each on its
     next run: which epics/sub-tasks it touches, what it changed in prd/plan/board, or one
     clarifying question. A change hitting an In-progress epic pauses that epic and states
     the cost BEFORE proceeding. Every change is a tracked ticket with a visible
     disposition — never a chat message that evaporates.

     Format:

     ## C<n> — <one-line request>            <!-- open tickets keep this heading -->
     Filed: <date>
     Request: <verbatim what you asked for>
     Touches: E<n>, E<n>.<m>
     Disposition: <what the PM changed, or the question it needs answered>

     Triaged tickets move under "## Done" with their disposition kept. -->

## Done
