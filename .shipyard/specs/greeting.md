# [Greeting] Greeting

## Acceptance criteria
- `bash run-tests.sh` passes with the greeting case asserted.
- greeting.sh prints the greeting line and nothing else.

## Proof of done
Run `bash greeting.sh` and read the line.
