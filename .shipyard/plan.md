# Plan — <project>

<!-- Written by the Shipyard Architect (Step 4) from prd.md. Every item here traces
     back to a line in prd.md; the consistency lens fails the plan otherwise.
     Epics land on board.md; the detail lives in specs/<epic>.md. -->

## Architecture

<!-- The shape of the system on the chosen stack: components, data flow, the
     2–3 approaches considered and why this one. -->

## Epics

<!-- One block per epic, in dependency order. Mirror the ids onto the board.

### [E1] <title>

- **Delivers:** what the user can do that they could not before.
- **PRD trace:** prd.md §Scope → <line>
- **Depends on:** — (or E<n>)
- **User-facing:** yes ⇒ design gate (mockup + Accept) before build; no ⇒ record the skip reason.
- **Acceptance criteria:** machine-checkable statements, one per line.
- **Proof of done:** the named observable thing that will demonstrate it (a test, a command's
  output, a flow in the preview).
- **Sub-tasks:** E1.1 … (each with its own criteria in specs/<epic>.md)
-->

## Risks

<!-- What could make this plan wrong, and what would tell us early. -->

## Deferred

<!-- In the PRD, deliberately not in this plan — with the reason. The scope-creep
     lens checks the plan against this list. -->
